How to run application:

1. Install requirements manually or (better!) use virtual environment
2. Set environment variables (in terminal)
	Mac OS: export FLASK_APP=demo.py
	Windows: set FLASK_APP=demo.py
3. Run it: ‘flask run’ (in terminal)